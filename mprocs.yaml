procs:
  inngest:
    cmd: ["bun", "run", "inngest:dev"]

  prisma-studio:
    cmd: ["bun", "--bun", "run", "prisma", "studio"]

  next:
    env:
      NODE_OPTIONS: "--trace-warnings"
    cmd: ["bun", "run", "dev"]
