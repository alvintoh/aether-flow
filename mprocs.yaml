procs:
  inngest:
    cmd: ["bun", "run", "inngest:dev"]

  drizzle-studio:
    cmd: ["bun", "drizzle-kit", "studio"]

  next:
    env:
      NODE_OPTIONS: "--trace-warnings"
    cmd: ["bun", "run", "dev"]
